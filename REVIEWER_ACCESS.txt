TEMPORARY FILE FOR REVIEWER ACCESS
====================================

This file contains information for reviewers to access the application.
It should be removed after the review is complete.

DJANGO SECRET KEY (For Testing Only):
django-insecure-_jq_2qm*1wl&p-guyz1tg2khc(-q0$h7^)%g%jj29v7%p@s+dv

NOTE: This is the default development secret key included in the repository.
For production use, generate a new secret key using:
python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'

ENVIRONMENT SETUP FOR TESTING:
-------------------------------

Method 1: No environment file needed (uses defaults)
Just run: python manage.py runserver

Method 2: Using .env file (recommended)
1. Copy .env.example to .env
2. The default values will work for testing
3. Run: python manage.py runserver

DOCKER TESTING:
---------------
No environment variables required for basic testing.
The application will use secure defaults.

To run with Docker:
docker build -t sticky-notes-app .
docker run -p 8000:8000 sticky-notes-app

ADMIN ACCESS:
-------------
After running migrations and creating a superuser, you can access:
- Admin panel: http://127.0.0.1:8000/admin/
- Use the credentials you create with: python manage.py createsuperuser

SECURITY NOTES:
---------------
- The SECRET_KEY in this file is ONLY for development/review
- Never use this key in production
- The .gitignore file prevents .env files from being committed
- Always generate new keys for production deployments

This file should be deleted once the review is complete and no resubmission is required.
